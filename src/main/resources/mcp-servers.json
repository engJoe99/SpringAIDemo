{
  "mcpServers": {

    "fetch": {
      "command": "python",
      "args": ["-m", "mcp_server_fetch"]
    },

    "filesystem": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ]
    }
  }

}
