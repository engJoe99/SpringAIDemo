spring:
  application:
    name: SpringAIDemo


  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: mistral
        options:
          max-tokens: 500

server:
  port: 8080
